# 🚀 كيفية تشغيل موقع Trend

## ⚡ الطريقة السريعة (الأسهل):

### 1️⃣ شغّل السيرفر:
- **اضغط مرتين** على الملف: `server/start-server.bat`
- انتظر حتى ترى: `🚀 Trend API running on http://localhost:5000`
- **اترك هذه النافذة مفتوحة!**

### 2️⃣ شغّل الواجهة:
- **اضغط مرتين** على الملف: `client/start-client.bat`
- انتظر حتى ترى رابط مثل: `http://localhost:5173`
- **اترك هذه النافذة مفتوحة!**

### 3️⃣ افتح المتصفح:
- اذهب إلى: **http://localhost:5173**

---

## 🔧 الطريقة اليدوية (إذا لم تعمل الطريقة السريعة):

### شغّل السيرفر:
```bat
cd C:\Users\hamod\Desktop\trend\server
npm install
npm start
```

### شغّل الواجهة (نافذة CMD جديدة):
```bat
cd C:\Users\hamod\Desktop\trend\client
npm install
npm run dev
```

---

## ❌ إذا ظهر خطأ:

### "npm is not recognized"
- تأكد أن Node.js مثبت
- أعد فتح CMD بعد التثبيت
- أو أعد تشغيل الجهاز

### "Port 5000 is already in use"
- أغلق البرنامج الذي يستخدم البورت 5000
- أو غير PORT في ملف `server/.env`

### "Cannot find module"
- نفّذ `npm install` في مجلد `server` و `client`

---

## ✅ للتحقق من أن كل شيء يعمل:

1. افتح: http://localhost:5000/api/health
   - يجب أن ترى: `{"ok":true,"message":"Trend API healthy"}`

2. افتح: http://localhost:5000/api/products
   - يجب أن ترى قائمة المنتجات

3. افتح: http://localhost:5173
   - يجب أن ترى الصفحة الرئيسية مع المنتجات

---

## 📞 إذا استمرت المشكلة:

1. تأكد أن Node.js مثبت: `node -v`
2. تأكد أن npm مثبت: `npm -v`
3. تأكد أن البورت 5000 و 5173 غير مستخدمين
4. تحقق من رسائل الخطأ في CMD

