# Backend environment variables
# Copy this file to server/.env and adjust values before deploying

# Node.js/Express server port
PORT=5000

# Admin dashboard token required in x-admin-token header
ADMIN_TOKEN=changeme

# WhatsApp number (without +) used for order confirmation links
OWNER_PHONE=966500000000

# (Optional) Absolute or hosted URL where the backend is served
PUBLIC_BASE_URL=https://trend-backend.example.com


# Frontend environment variables
# Copy this section to client/.env when building the React app

# Base URL of the deployed backend API
VITE_API_URL=https://trend-backend.example.com

# WhatsApp number override for frontend-only link generation (fallbacks to OWNER_PHONE)
VITE_WA_NUMBER=966500000000
